---
import Title from "@components/Title.astro";
import { Image } from "astro:assets";
import aboutImg1 from "@assets/images/about-img-1.webp";
import Highlight from "@components/Highlight.astro";
import { SECTIONS_IDS } from "@utils/constants";
---

<section id={SECTIONS_IDS.about} class="flex flex-col gap-12">
  <Title>About Me</Title>
  <article class="flex flex-col items-center gap-10 lg:flex-row">
    <div class="flex flex-[2] flex-col gap-2">
      <p class="text-justify text-gray-500 dark:text-gray-400">
        I'm a <Highlight>passionate software developer</Highlight> with an engineering
        background who thrives in web development. While <Highlight
          >comfortable across the full stack</Highlight
        >, I have a special love for <Highlight
          >crafting intuitive frontend experiences</Highlight
        >. In the workplace, I find great fulfillment in <Highlight
          >mentoring colleagues</Highlight
        >.
      </p>
      <p class="text-justify text-gray-500 dark:text-gray-400">
        Outside of work, I contribute to <Highlight
          >open-source projects</Highlight
        >, build personal applications that challenge me to <Highlight
          >expand my technical expertise</Highlight
        >, try to keep up with the latest web development trends, and
        <Highlight
          >explore new technologies to push creative boundaries</Highlight
        >.
      </p>
      <p class="text-justify text-gray-500 dark:text-gray-400">
        When unplugged from code, you'll find me cruising on my longboard,
        immersed in video games, or enjoying films. My free time is also happily
        spent with my six pets - three dogs and three cats. 🐶🐱
      </p>
    </div>
    <figure class="flex flex-1 flex-col gap-2 overflow-hidden rounded-md">
      <Image
        src={aboutImg1}
        width={600}
        alt="A photo of me with Lila and Limón"
        class="h-full w-full rounded-md object-cover"
        loading="lazy"
        decoding="async"
      />
      <figcaption>
        <p class="text-sm text-gray-400">
          <em
            >A special moment with <span
              id="lila"
              class="cursor-pointer transition-colors duration-300">Lila</span
            > and <span
              id="limon"
              class="cursor-pointer transition-colors duration-300">Limón</span
            >, two of my pups.</em
          >
        </p>
      </figcaption>
    </figure>
  </article>
</section>

<script>
  import { completeEasterEgg } from "@utils/functions.js";

  const lila = document.getElementById("lila") as HTMLElement;
  const limon = document.getElementById("limon") as HTMLElement;

  let clickCount = 0;

  lila.addEventListener("click", () => {
    lila.classList.toggle("text-fuchsia-500");

    if (clickCount < 2) clickCount++;

    if (clickCount === 2) {
      completeEasterEgg("lila-limon");
    }
  });

  limon.addEventListener("click", () => {
    limon.classList.toggle("text-amber-500");

    if (clickCount < 2) clickCount++;

    if (clickCount === 2) {
      completeEasterEgg("lila-limon");
    }
  });
</script>
